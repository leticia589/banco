const funcSoma = require('./modulos/funqsoma.js');
const listPessoas = require ('./modulos/pessoas');
console.log(funcSoma(10,5));
console.log(listPessoas);
function mediaIdade(lista) {
    let resuly = 0;
    for (let aux =0; aux<lista,lenght; aux++) {
        result+=lista[aux].idade
 
   }
   return result/lista.lenght;
}
console.log(mediaIdade(listPessoas) );


